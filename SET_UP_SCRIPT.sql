-- SET UP SCRIPT
-- RUN FIRST
-- Set Autocommit
SET AUTOCOMMIT OFF;
-- To view server output:
SET SERVEROUTPUT ON;
-- FORMATTING NOTES
SET LINESIZE 170;
-- If date_time column format is too wide precede the query thus with the command:
COLUMN date_time FORMAT A25 WORD WRAPPED
/
-- If expiry column format is too wide precede the query thus with the command:
COLUMN expiry FORMAT A25 WORD WRAPPED
/
--
-- END of SCRIPT
--